import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tst.storybooks',
  appName: 'Story Books',
  webDir: 'build',
  bundledWebRuntime: true
};

export default config;
